[project]
name = "gl-pipeline-monitor"
version = "0.1.0"
description = "Interactive TUI for monitoring GitLab MR pipelines."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "textual>=8.0.0",
    "pfzy>=0.3.4",
]
